# Dependencies
node_modules/

# Build output directories (Generated packages/executables go here)
out/
dist/

# Runtime Data / Logs
*.log
logs/
*.pid
*.seed
*.pid.lock
lib-cov/
coverage/
.nyc_output/
gameState.json # Don't commit user runtime state
data/          # Don't commit the fallback data directory (this covers the C:\thorgrid-electron\data fallback)

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE / Editor specific
.vscode/
*.sublime-project
*.sublime-workspace
.idea/
*.iml

# Environment variables
.env
.env.*
!.env.example

# Electron Forge state (optional, usually safe to ignore)
.electron/

# Ignore build artifacts and temporary files
build/*.png
build/*.html
# Or just ignore everything in build if you don't commit anything from there
# build/